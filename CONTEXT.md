# Lumina（流光）画布 Agent

本上下文描述 Lumina（流光）的画布创作、Web 数据归属，以及 Agent 产物与画布之间的关系。

## Web 数据与生成

**产品形态基线**：Lumina 是闭源、本机安装和本机运行的产品；一次性安装器只负责安装已编译的发布物。安装后由隐藏的本地运行时按需提供 Web 页面、GenerationGateway 和 Agent bridge，不展示桌面画布窗口。Chrome 或 Codex 内置浏览器都可承载 Web 画布；Codex Lumina 插件默认使用 Codex 内置浏览器，不自动切换外部 Chrome。普通用户不需要获取或运行 Lumina Git 源码。

**功能等价基线**：`v0.2.37` 中已经存在的用户可见业务能力、异常行为和实验状态集合。Web 完整替代以这些行为逐项可验证为准，不以源码结构相同或通用节点近似实现为准。

**Web 项目库**：由当前产品选定的浏览器上下文中、当前安装稳定的 Lumina canonical Origin 持有的项目、画布和历史事实集合。Codex Lumina 插件默认在 Codex 内置浏览器中访问该项目库；外部 Chrome 与内置浏览器之间的 IndexedDB、历史和资产连续性未经绑定验证前不得假定。生成服务、桥接服务和外部 Agent 都不拥有这些事实。_避免_：Agent 端独立项目库、会话级项目库、云端项目库、服务端项目库。

**本地运行时**：由安装器部署的闭源、已编译后台服务。它只负责提供构建后的 Lumina Web 页面、GenerationGateway、受控 Agent bridge 和稳定本地入口；不展示桌面画布、不拥有项目数据，也不替代 Chrome。它可由手动入口或画布启动 Skill 按需启动和复用。

**本地 Lumina 入口**：由本地运行时在首次安装后为该安装实例分配并持久记录的非通用 loopback Origin。安装器在安装目录之外登记实际 runtime 路径，Codex 插件据此兼容 Windows 自定义安装目录和 macOS 非默认目标卷。首次端口冲突时本地运行时自动选择另一个可用地址；手动 `lumina://open` 入口和 Agent 都经本地运行时解析已记录的地址。正常启动不得静默更换 Origin；既有地址被无关进程占用时进入明确修复，而不产生新的 Web 项目库。

**浏览器资产**：由 Web 项目或保留历史引用、长期保存在浏览器中的图片、音频或视频内容。节点只引用其稳定身份，不把临时展示地址当作资产。_避免_：Object URL、远端结果 URL。

**生成网关（GenerationGateway）**：本地运行、代表画布向选定 AI 供应商提交、查询和取消生成任务的受控边界。它可短期处理完成请求所需的任务映射和媒体，但不拥有项目、画布、历史或长期资产；当前 local-first 范围不要求模型推理也在本机。_避免_：项目后端、云端项目服务。

**临时生成介质**：为供应商访问、格式转换或结果回收而短期托管的媒体副本。它受明确的生命周期约束，不是浏览器资产，也不能成为项目持久化事实。

## Agent 与会话

**画布 Agent**：理解用户创作意图并在画布上形成图片编辑结果的协作者。_避免_：聊天机器人、自动化脚本。

**外部 Agent**：运行在 Lumina 之外、通过 Lumina 授权的标准能力接口读取或操作当前活动项目的 Agent。它不拥有项目、画布或权限事实。_避免_：MCP、外部 MCP。

**Lumina MCP 服务**：Lumina 向同机外部 Agent 暴露当前活动项目画布能力的标准接口。它是受控接入边界，不是 Agent，也不是画布事实源。_避免_：外部 Agent、Canvas Agent。

**画布启动 Skill**：当用户明确要求打开或使用 Lumina 时，由 Codex 调用 Lumina MCP 服务，唤起或复用本地运行时，并在 Codex 内置浏览器中打开或聚焦本地 Lumina 入口。它只建立或恢复会话，不授予项目写权限或生成授权。_避免_：自动创作触发器、写入授权。

**会话**：一个项目内持续的 Agent 对话及其创作记录。会话不跨项目共享画布引用。

**Agent 回合**：会话中从一次用户请求开始，到建议、执行和结果状态结束的一次完整工作单元。_避免_：消息。

## 画布意图与引用

**画布引用**：用户明确交给 Agent 使用的节点、资源或当前选择。没有明确引用的画布内容不属于回合上下文。

**提示词块（Prompt Block）**：保存可读、可继续编辑的图片创作意图，以及该意图引用的画布资源。_避免_：提示词字符串、隐藏提示词。

**输入节点**：为一次图片编辑提供输入的既有画布节点。多图编辑时，每个输入节点都是同等的参考来源；“输入节点”不表示主图或首张图。输入节点在 Agent 回合中保持不被覆盖。

**结果节点**：由一次图片编辑产生、代表新资产的画布节点。结果节点与输入节点保持可追溯关系。

## 受控执行

**变更集（CanvasChangeSet）**：Agent 对画布提交的一组有边界的创建、更新、移动和连线意图。通过活动项目、画布版本和能力白名单校验后，它会直接作为一个可撤销的整体执行。

**外部画布变更**：外部 Agent 通过 Lumina MCP 服务提交的 `CanvasChangeSet`。启用外部访问代表用户授予当前活动项目的受限写权限；校验失败的变更不写入画布，校验成功的全部操作直接形成一个可整批撤销的画布变更。

**外部画布动作**：外部 Agent 通过 Lumina MCP 服务请求 WebView 执行的受控非变更集操作。当前只包括使用既有图片管线导入参考图、运行已存在的图片生成节点，以及按明确节点 ID 读取压缩结果预览；它不引入新的业务节点或任务系统。

**占位节点**：外部图片任务完成前，代表预期结果位置和运行状态的临时结果节点。

**画布任务绑定（CanvasJobBinding）**：把一次外部图片任务与其输入节点、提示词块、占位节点和结果节点关联起来的记录。

**整批撤销**：通过一次撤销恢复一个 Agent 变更集执行前的节点和连线状态。

**Agent 网关（AgentGateway）**：Agent 回合连接文本模型、流事件和取消能力的受控边界。它不拥有画布事实，也不直接修改画布。

**任务绑定状态**：一次图片任务从排队、运行到成功、失败、取消或失联的生命周期状态。状态变化不改变输入节点。

**可恢复查询故障**：已取得供应商任务 ID 的可恢复任务，在查询结果时遇到网络或暂时性服务错误的运行状态。系统只重新查询原任务，采用有限的指数退避；达到阈值后保留任务绑定并要求用户显式重新查询，绝不重新提交生成请求。

**失联任务（orphaned job）**：任务可能仍在供应商侧运行，但其本地回合已停止或无法安全接收结果的任务。失联任务的迟到结果不得自动写入画布。

**回合重试**：使用原回合的输入快照和任务绑定重新查询或提交失败任务的操作。重试不重新创建已经存在的画布对象。

**多图引用**：一次图片编辑回合中按明确顺序选择的多张参考图片；它们共同参与一个结果生成，不等同于对每张图片分别执行的批处理。连接顺序也是提示词标签顺序，依次对应“图片 1”“图片 2”等引用。

**澄清待定**：Agent 无法从当前明确引用和用户请求安全确定图片、模型或节点结构时的回合状态。澄清待定状态不会写入画布。

**图片编辑 Skill**：把用户的自然语言图片修改意图转换为提示词和受限图片编辑动作的内置创作能力。P0 只提供一个固定版本。

**有效提示词**：图片节点运行时使用的提示词，由已连接文本生成节点的有效文本按用户确定的顺序排列、图片节点自身的手工提示词置后，并以空行分隔。一次已提交任务保存合并后的提示词快照。

**提示词组**：同一 Agent 请求产生、用作提示词来源的多个文本生成节点的有序集合。提示词组不等同于一次批量生图任务。

## 文本工作流

**文本生成节点（TextGenerationNode）**：画布上一等的通用文本来源节点，可结合上游文本、参考图片和本地输入生成可供下游继续使用的文本。_避免_：PromptNode、文本注释节点。

**文本注释节点**：只承载用户笔记的非执行节点，不参与文本或图片工作流。_避免_：文本节点、文本生成节点。

**有效文本**：文本生成节点当前提供给下游的文本。首次成功生成前由上游有效文本按用户确定的顺序排列、本地输入置后，并以空行分隔；首次成功生成后由当前结果文本提供，直到结果文本被替换或清除。

**上游上下文**：通过连线进入文本生成节点的多个有效文本和图片资产。上游变化不会自动触发当前节点或下游节点运行。

**通用输入端口**：文本生成节点和图片生成节点接收文本与图片的单一可见入口，连线仍按来源输出识别和校验内容类型。文本与图片分别形成有序列表，新输入默认追加，用户可在节点内调整各自顺序。

**图片输入集**：文本生成节点通过通用输入端口收集的有序图片资产，产品级上限为十张。所选模型的已知图片数量上限仍会单独限制节点运行。

**文本模型目录**：一个文本供应商可被发现并由用户手工补充的模型身份集合。目录项只声明模型 ID 和可选名称，不因出现在目录中就获得视觉输入、图片数量或思考等级能力。

**节点可用文本模型**：用户从各文本模型目录中选出、允许画布节点使用的模型子集。节点只能从该子集选择模型；已有选择被移出子集后保持原值但变为不可用，不自动回退。

**文本模型选择**：文本生成节点对一个全局供应商配置及其节点可用模型之一的节点级选择。选择不因供应商或模型暂时不可用、与输入不兼容而自动改变；已知不兼容会阻止节点运行。

**文本供应商配置**：一组全局共享的文本模型连接信息、模型目录和节点可用模型。文本生成节点引用供应商及模型，不复制供应商凭据。

**图片润色配置**：图片、分镜和视频提示词润色共用的一组文本供应商、模型和思考等级，以及图片润色模板。它与文本生成节点自身的模型选择及默认文本模型相互独立。

**文本润色配置**：文本生成节点本地提示词润色专用的一组文本供应商、模型、思考等级和文本润色模板。它与图片润色配置独立；润色只改写本地提示词，不改写上游上下文或已生成结果。

**默认文本模型**：用户最近一次在文本生成节点中选择的供应商和模型，只用于初始化之后新建的文本生成节点。它不是提示词润色模型，已有节点始终保留各自的文本模型选择。

**文本模型能力**：系统掌握的模型级硬约束，例如最大图片数或允许的思考等级，不是需要用户配置的能力开关。文本模型默认被视为支持视觉输入；只有已有可靠声明的硬限制才提前阻止运行，其余兼容性由供应商调用结果判定，禁止根据模型 ID 猜测。

**思考等级**：文本模型运行的推理强度参数，当前统一候选为 `none`、`minimal`、`low`、`medium`、`high`、`xhigh` 和 `max`，未设置表示使用供应商默认值。文本模型能力可进一步限制候选；思考等级不授权展示或向下游传递隐藏推理过程。

**执行链路**：由数据连线组成、不得形成有向环的节点依赖关系。连线与上游内容变化只改变运行时输入，不自动调用任何模型。

**文本生成运行**：用户基于当前上游文本、本地输入和图片发起的一次文本模型调用，每个节点同时至多存在一个。重复运行始终重新读取当前输入，不把已有生成结果作为下一次输入。

**运行输入快照**：文本生成运行开始时确定的有序文本和图片集合。运行期间画布输入可以继续变化，但该运行仍按其快照完成。

**结果文本**：文本生成节点在首次非空成功运行后持有、可由用户继续修订且只按纯文本展示的 UTF-8 文本。完整成功响应会一次性替换其内容，人工修订会立即更新其内容；人工删空等同于清除结果，后续运行、输入变化、运行失败或停止均不自行改变它。

**停止文本运行**：使当前文本生成运行不再有资格写入节点结果的用户操作。供应商侧无法取消时，停止后的迟到结果仍必须被忽略。
